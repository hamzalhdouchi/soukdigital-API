spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/soukdigital
    username: postgres
    password: 37533753

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      password:

  mail:
    host: localhost
    port: 1025   # MailHog / Mailpit for local dev

jwt:
  secret: dev-secret-key-at-least-256-bits-long-for-hs256-algorithm-ok
  expiration: 86400000
  refresh-expiration: 604800000

cors:
  allowed-origins: http://localhost:3000,http://localhost:3001

logging:
  level:
    ma.soukdigital: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
